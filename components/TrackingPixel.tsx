// JJ-08: Meta (Facebook/Instagram) retargeting pixel placeholder.
// Insert the live snippet below when issued by the project lead.
// Pattern supports additional pixels by adding sibling <script> blocks here.

export function TrackingPixel() {
  return (
    <>
      {/*
        JJ-08 — PENDING: Meta retargeting pixel
        Replace this comment block with the issued snippet, e.g.:
        <script dangerouslySetInnerHTML={{ __html: `...fbq('init', 'PIXEL_ID')...` }} />
      */}
    </>
  );
}
